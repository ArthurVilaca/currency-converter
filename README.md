### Currency converter

#### how to run

    cd ./backend
    docker-compose build
    docker-compose up
    cd ./frontend
    docker-compose build
    docker-compose up
    open localhost:3000

#### used libraries

- backend
    - nest.js as main framework

- frontend
    - react and axios
